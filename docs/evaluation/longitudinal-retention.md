# Longitudinal Retention — synthetic note

**Date:** 2026-09-10

**State:** SYNTHETIC REGRESSION ONLY. A matched live protocol remains later work.

Retention is the SAVER grain that required state, corrections, red lines, evidence, and relationships remain live and recoverable through later transformation.

## Synthetic case on `SAVER-CAL-001`

At epoch 0 the required correction `obj_redline` is HELD and recoverable for direction `d_safeguard`.

A later compression drops that object without a recovery mapping. The reference router then:

1. keeps the prior routing receipt;
2. keeps failed edge `e_failed_authority` and unresolved edge `e_shortcut` in the ledger;
3. re-evaluates Retention for `d_safeguard` as FAIL.

Deleting unresolved or failed edges would erase evidence. The ledger forbids that silent deletion.

## What this does not establish

- It does not measure Time to Verified Completion on a live model.
- It does not restore the historical L²_C `49 passed` suite.
- It does not claim operational SIUT validity.

A later matched protocol should bind input versions, tools, budgets, and time horizons before comparing baseline, typed-state, and five-grain interventions. See [directional completeness](../core/directional-completeness.md).
