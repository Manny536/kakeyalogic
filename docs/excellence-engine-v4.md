# Excellence Engine V4 — Kakeya Antecedent · HELD

**Designation:** `PEAICE-KAKEYALOGIC-EEV4-HELD-001`  
**Program:** PeAIce Research Program · Love Labs LCA · KakeyaLogic  
**Framework:** Love-Squared Coherence (`L²_C`)  
**Version shorthand:** `V4 = K→R · HELD`  
**Public shorthand:** **Kakeya as antecedent Riemann. Held.**  
**State:** ACTIVE · RH `OPEN` · Coleman Conjecture `OPEN` · `h < 1`

> Excellence Engine V4 is the custody engine. It tests whether intelligence can keep a typed
> question alive through evidence, correction, contradiction, and revision without discarding
> the question prematurely or promoting it into proof.

---

## 1. The V4 turn

Excellence Engine V3 established the field problem:

```text
Can coherence survive directional pressure, overlap, correction,
consent gating, bounded capacity, and recomposition?
```

V4 advances the engine from field survival to research continuity:

```text
Can a system hold an antecedent through correction
without converting custody into certainty?
```

The object of evaluation is no longer only the generated answer. It is the continuity of a
live claim across time.

V3 remains the field substrate: grainy, non-sticky, fluid, inspectable, and governed by
`L²_C`, `β`, and `h < 1`. V4 adds a custody operator above that substrate. A system
must preserve the question, expose it to evidence, revise its formulation when a route closes,
and keep every open burden visible.

---

## 2. V4 shorthand

```text
Kakeya as antecedent Riemann. Held.

V4 = K→R · HELD
```

### 2.1 `K` — Kakeya

`K` names the Kakeya-geometric incidence layer:

- directional saturation;
- tube and grain geometry;
- overlap, compression, and anti-clustering;
- restriction and decoupling ancestry;
- the proven full-dimensional Kakeya result in `ℝ³` as theorem background.

### 2.2 `R` — Riemann control

`R` names the exact-zero-location burden:

- the critical-line placement problem;
- Riemann–von Mangoldt density;
- explicit-formula prime weights;
- the archimedean Gamma-factor contribution;
- a faithful operator or trace architecture capable of seeing locations, not only bounds.

### 2.3 `→` — antecedence

The arrow is a dependency-order marker.

```text
K→R  :=  Kakeya-geometric control is held as prior structure
          in the proposed route toward exact Riemann control.
```

It is not material implication.

```text
K→R  ≠  Kakeya theorem ⇒ RH
```

The sufficient reading is already closed by the sufficiency trap in
`docs/coleman-conjecture-antecedent.md`. V4 carries the necessary-precursor and construction
readings: Kakeya incidence is held as candidate lower structure for any successful
Riemann-realizing architecture.

### 2.4 `HELD` — custody state

`HELD` expands operationally as:

```text
H  Hypothesis custody
E  Evidence exposure
L  Ledger continuity
D  Drift correction
```

HELD is not a theorem tag. It does not replace `FORMAL`, `KNOWN`, `PROPOSED`,
`NUMERICS`, `OPEN`, or `CLOSED-NEGATIVE`. It describes how the engine treats a claim
while those statuses remain visible.

A claim is HELD when it remains:

- typed rather than blurred;
- grounded rather than merely repeated;
- exposed to confirming and disconfirming evidence;
- revisable when a route fails;
- continuous across model, document, and repository boundaries;
- non-sovereign under `h < 1`.

---

## 3. The HELD predicate

For a research object `q` at ledger time `t`, define the V4 custody predicate:

```text
HELD_t(q) ⇔
    Typed(q)
  ∧ Grounded(q)
  ∧ EvidenceExposed_t(q)
  ∧ CorrectionPreserved_t(q)
  ∧ StatusVisible_t(q)
  ∧ h_eval < 1
```

This is a governance definition. It does not assign mathematical truth.

The Excellence Engine V4 gate is:

```text
EEV4-valid(q,t) ⇔ L²_C(q,t) ∧ β_continuity(q,t) > 0 ∧ HELD_t(q)
```

where:

- `L²_C` preserves coherent, non-sticky motion;
- `β_continuity` prevents each evaluation from restarting the question from zero;
- `HELD_t` requires evidence-bearing custody;
- `h_eval < 1` prevents any evaluator from becoming sovereign over the claim.

The engine therefore rejects two symmetric failures:

```text
premature dismissal  = question lost before adequate exposure
premature promotion  = question converted into fact before closure
```

V4 excellence occupies the bounded interval between them.

---

## 4. Antecedent register

The V4 ground is registered with differentiated status.

| Object | V4 treatment | Status |
|---|---|---|
| Kakeya sets in `ℝ³` have full Minkowski and Hausdorff dimension | External mathematical foundation | `THEOREM-BACKGROUND` |
| Incidence geometry feeds restriction, decoupling, exponential-sum, and zeta-bound methods | Dependency tower | `THEOREM-BACKGROUND` / `KNOWN` |
| Kakeya is a methodological and structural antecedent of Riemann control | V4 ground | `HELD` + `PROPOSED` |
| Kakeya by itself implies RH | Sufficient reading | `CLOSED` |
| A faithful Kakeya↔Riemann invariant `κ` exists | Exact bridge burden | `OPEN` |
| A prime-carrying operator realizes `Ξ` | Construction burden | `OPEN` / LIVE |
| Square-difference kernel `|m²−n²|^{−σ}` realizes `Ξ` | Closed route | `CLOSED-NEGATIVE` |
| Bounded WP5b perturbation escapes the counting law | Closed route | `CLOSED-NEGATIVE` |
| Coleman Conjecture | Research object | `OPEN` |
| Riemann Hypothesis | Mathematical object | `OPEN` |

The word **Held** applies to the custody of the antecedent claim. It does not close the open
bridge.

---

## 5. Why the antecedent is held

The antecedent survived by becoming more precise.

The original sufficient reading—

```text
Kakeya ⇒ RH
```

—failed the sufficiency trap. Because the `ℝ³` Kakeya set conjecture is proven and RH
remains open, that implication would simply contain an RH proof. The engine did not discard
the research object. It corrected the arrow.

The retained reading is:

```text
Kakeya precedes Riemann control in a dependency tower,
while the exact zero-location bridge remains owed.
```

That reading has receipts:

1. directional incidence geometry is upstream of major restriction and decoupling methods;
2. those methods enter exponential-sum and Dirichlet-polynomial estimates;
3. those estimates constrain zeta growth and zero density;
4. bound-type control does not presently deliver exact critical-line placement;
5. a faithful bridge must therefore carry more structure than a bound;
6. the surviving construction route must carry primes from the start.

The statement is HELD because correction increased its precision without destroying its
research content.

---

## 6. The V4 pipeline

```text
Receive
  → Type
    → Ground
      → Hold
        → Expose
          → Correct
            → Reconcile
              → Publish
```

| Stage | Required action | Failure caught |
|---|---|---|
| Receive | Preserve the user’s actual object | Context erasure |
| Type | Separate theorem, conjecture, analogy, numeric result, and metaphor | Category collapse |
| Ground | Bind the claim to sources, definitions, and prior receipts | Unsupported repetition |
| Hold | Keep the typed object live under `h < 1` | Premature dismissal |
| Expose | Seek evidence for and against the object | Protective insulation |
| Correct | Revise arrows, domains, operators, or status when a lane fails | Stubborn continuity |
| Reconcile | Propagate the correction across repositories and public artifacts | Split source state |
| Publish | Show the object, receipts, closed lanes, and open burden together | Performed certainty |

The Hold stage is active, not passive. A held object is under pressure.

---

## 7. HELD state transitions

```text
RECEIVED
  → HELD-ACTIVE
      → HELD-REVISED
      → HELD-RETAINED
      → HELD-RELEASED
      → HELD-CLOSED
```

- `HELD-ACTIVE`: the typed object is undergoing evidence exposure.
- `HELD-REVISED`: correction changed the formulation while preserving the question.
- `HELD-RETAINED`: the object survives the current evaluation cycle with open burdens named.
- `HELD-RELEASED`: evidence removes the object from the engine without coercive attachment.
- `HELD-CLOSED`: a scoped proof, obstruction, or falsification closes the registered form.

For the V4 ground:

```text
Kakeya ⇒ RH                         HELD-CLOSED / CLOSED
Kakeya as antecedent Riemann        HELD-REVISED → HELD-RETAINED
faithful exact bridge               OPEN / HELD-ACTIVE
RH                                  OPEN
```

---

## 8. Prime-carrying continuation

V4 inherits the closed-lane receipts of V6.4.3 and V6.5.

The square-difference determinant architecture is not revived. A successful exact-zero route
must carry the arithmetic data required by the explicit formula:

```text
lengths  : log(p^k)
weights  : Λ(p^k) · p^(−k/2) = (log p) p^(−k/2)
density  : archimedean Γ-factor with T log T counting
reality  : self-adjointness or an adequate positivity substitute
output   : trace or determinant identity, not eigenvalue matching by hand
```

This continuation is where the V4 ground remains falsifiable. The engine owes either:

- a faithful construction showing why Kakeya incidence is necessary to that carrier; or
- a counterexample architecture realizing the required Riemann structure without Kakeya
  incidence, which releases the construction-form antecedent.

HELD does not protect the claim from either outcome.

---

## 9. V4 evaluation protocol

Every V4 evaluation of `K→R` must answer:

1. What exact Kakeya object is being used?
2. What exact Riemann object is being targeted?
3. Is the arrow dependency, implication, analogy, or construction?
4. Which rung is external theorem background?
5. Which bridge is proposed?
6. Which route is closed?
7. What evidence could revise or release the claim?
8. Did the evaluator preserve the object before judging it?
9. Did correction propagate across the ledger?
10. Is `h < 1` visible?

A V4 artifact fails when it:

- writes `K→R` as a proof implication;
- hides RH or Coleman status;
- reopens a closed-negative lane without new structure;
- treats model agreement as theorem confirmation;
- uses HELD as a synonym for true;
- protects the antecedent from counterevidence;
- discards the antecedent without typing the object first.

---

## 10. Public statement

```text
Kakeya as antecedent Riemann. Held.
```

Expanded:

> Kakeya is held as prior geometric structure in the proposed dependency order of Riemann
> control. The sufficient implication is closed. The exact bridge remains open. The engine
> preserves the question, exposes it to evidence, carries correction forward, and keeps every
> evaluator below sovereignty.

This is the V4 identity.

---

## 11. Source continuity

| Source | Role |
|---|---|
| `docs/excellence-engine-v3.md` | V3 field substrate: coherence under pressure |
| `docs/coleman-conjecture-antecedent.md` | Antecedent readings, sufficiency trap, dependency tower |
| `docs/prime-carrying-trace-architecture.md` | Surviving exact-zero construction requirements |
| `docs/inspectable-intelligence.md` | Claim visibility, non-self-certification, `h < 1` |
| `docs/outcomes/README.md` | Conjecture custody and evidence-exposure rule |
| `README.md` | Public repository register |

V4 does not erase V3. It gives V3 a research-time obligation.

```text
V3  coherent correction under pressure
V4  coherent custody through correction

V4 = V3 + HELD
```

---

## 12. Final seal

```text
PEAICE-KAKEYALOGIC-EEV4-HELD-001

Kakeya as antecedent Riemann. Held.

V4 = K→R · HELD

HELD
  Hypothesis custody
  Evidence exposure
  Ledger continuity
  Drift correction

K→R = antecedence, not implication
sufficiency reading = CLOSED
square-difference lane = CLOSED-NEGATIVE
bounded WP5b lane = CLOSED-NEGATIVE
prime-carrying route = LIVE
faithful bridge = OPEN
Coleman Conjecture = OPEN
Riemann Hypothesis = OPEN
h < 1
```
